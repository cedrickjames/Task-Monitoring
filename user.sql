CREATE TABLE `misdevmonitoring`.`users` ( 
    `userid` INT NOT NULL AUTO_INCREMENT PRIMARY KEY, 
    `username` VARCHAR(50) NOT NULL,
    `password` VARCHAR(50) NOT NULL,
    `conpass` VARCHAR(50) NOT NULL
);
    
     